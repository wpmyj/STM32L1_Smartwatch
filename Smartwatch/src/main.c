#include "main.h"

/*void bluetooth_Init(uint16_t baudrate);
void sendByte(uint8_t data);
uint8_t USART1_flagStatus(uint16_t flag);*/

char krneki[3];
int a = 0;

void main(){

    /*bluetooth_Init(9600);

    while(!USART1_flagStatus(USART_SR_TXE));
    sendByte('A');
    while(!USART1_flagStatus(USART_SR_TXE));
    sendByte('T');
    a = 100;
    while(!USART1_flagStatus(USART_SR_RXNE));
    krneki[0] = USART1->DR;
    while(!USART1_flagStatus(USART_SR_RXNE));
    krneki[1] = USART1->DR;
    a = 1000;
    for(;;);*/

}
